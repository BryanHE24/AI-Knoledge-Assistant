# AI Knowledge Assistant API

Production-ready RAG API built with:

- Python
- FastAPI
- OpenRouter
- FAISS

## Goal

Expose a retrieval-augmented generation backend through scalable API endpoints.

## Planned Features

- document retrieval
- semantic search
- `/ask` endpoint
- structured responses
- Docker deployment# AI Knowledge Assistant API

Production-ready RAG API built with:

- Python
- FastAPI
- OpenRouter
- FAISS

## Goal

Expose a retrieval-augmented generation backend through scalable API endpoints.

## Planned Features

- document retrieval
- semantic search
- `/ask` endpoint
- structured responses
- Docker deployment